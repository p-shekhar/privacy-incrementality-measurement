[project]
name = "privacy-robust-incrementality-measurement"
version = "0.1.0"
description = "Notebook-first experiments for privacy-robust incrementality certification under signal loss."
requires-python = ">=3.10"
dependencies = [
  "matplotlib>=3.7",
  "numpy>=1.24",
  "pandas>=2.0",
]

[tool.ruff]
line-length = 120

